# 길담 추천 결과 디자인 규칙

추천 결과 화면의 구현 기준은 Figma `추천 코스` 프레임이다.

- 기준 파일: `tFvt1Fg5hKd5poUOTanpWv`
- 기준 노드: `19:424`
- 기준 프레임: 390 × 844px
- 링크: https://www.figma.com/design/tFvt1Fg5hKd5poUOTanpWv/Untitled?node-id=19-424

## 1. 슬라이싱 규칙

```text
RecommendationResults
├─ PageHeader
│  └─ BackButton
└─ ResultsContent
   ├─ ResultHeading
   ├─ ConditionSummary
   │  ├─ ConditionChipList
   │  └─ RecommendationCriteria
   ├─ FeaturedCourseCard
   │  ├─ RankLabel
   │  ├─ CourseImage
   │  │  └─ ImageCredit
   │  └─ CourseCardBody
   │     ├─ CourseTitle
   │     ├─ CourseMetrics (4 columns)
   │     ├─ CourseTagList
   │     ├─ RecommendationReasons
   │     └─ CourseDetailButton
   ├─ AlternativeCourseList
      └─ RankLabel + CourseCard × 1–2
   └─ ExclusionReasons
```

- 컴포넌트는 데이터 책임을 기준으로 나눈다. 카드 내부 장식만을 위한 컴포넌트는 만들지 않는다.
- 지표, 칩, 버튼은 반복 가능한 단위를 재사용한다.
- Figma는 대표 카드까지만 정의한다. 제품 명세의 대안 코스는 삭제하지 않고 대표 카드 아래에 같은 토큰으로 배치한다.

## 2. 레이아웃

| 영역 | 규칙 |
|---|---|
| 앱 배경 | `#FFFFFF` |
| 헤더 | 높이 64px, 중앙 제목은 시각적으로 숨기고 뒤로가기만 노출 |
| 본문 | 좌우 20px, 상단 16px, 주요 블록 간격 32px |
| 순위 라벨 | 카드 밖 상단에 `추천 1위`~`추천 3위` 텍스트로 표시 |
| 대표 카드 | 폭 100%, 모서리 30px, 테두리 없음, `0 0 10px rgba(0,0,0,.1)` |
| 카드 이미지 | 높이 213px, 카드 폭보다 넓게 채운 뒤 중앙 상단 기준으로 자름 |
| 카드 본문 | 패딩 16px, 항목 간격 24px |
| 이미지 출처 | 이미지 오른쪽·아래 10px에 흰색 70%로 표시 |
| 지표 | 같은 폭의 4열 중앙 정렬. `예상 시간` 열의 오른쪽 패딩은 12px |
| 핀뱃지 | 흰 배경, `#D3C7C0` 1px 테두리, 좌우 10px·상하 8px, pill 형태, 간격 5px |
| 귀가 행동 안내 | 귀가 상태 뱃지 아래에 보조 문구로 표시, 별도 칩으로 만들지 않음 |
| 추천 기준 | 조건 칩 아래에 접힌 `추천 기준 보기`로 배치하고 펼치면 취향·이동 부담·귀가 여유·지역성 4개 기준과 가중치를 표시 |
| 순위 비교 | `추천 기준 보기` 안에서 이동 부담의 도보·환승·왕복 이동 세부 비중과 2·3위가 갈린 이유를 한 문장으로 표시 |
| 상세 귀가 정보 | 요약 지표 다음에 배치하고 행동 안내·기준 일정·교통 안내 한 문장만 표시. 코스 전체 출처는 표시하지 않음 |
| CTA | 폭 100%, 시각 높이 40px, pill 형태 |

제목→조건 칩과 조건 칩→대표 카드의 간격은 각각 32px이다. 390px에서 본문 폭은 350px이다.

### 반응형

- 320–480px에서 가로 스크롤이 없어야 한다.
- 칩과 태그는 공간이 부족하면 다음 줄로 흐른다.
- 4열 지표의 순서는 유지한다. 모든 지표 값은 14px로 통일하고, `예상 시간`은 `10시간 10분`까지 한 줄로 표시한다.
- 480px보다 넓은 화면에서는 앱 셸 최대 폭 480px을 유지한다.

## 3. 색상과 효과

| 토큰 | 값 | 사용처 |
|---|---|---|
| Text primary | `#4D433D` | 제목, 지표 값, 섹션 제목 |
| Text secondary | `#706661` | 지표 레이블, 추천 이유 본문 |
| Chip background | `#FFFFFF` | 조건 칩, 코스 태그, 귀가 상태 뱃지 |
| Chip border | `#D3C7C0` | 핀뱃지 외곽선 |
| Chip text | `#706661` | 핀뱃지 텍스트 |
| Return available text | `#5B7F30` | `귀가 가능` 뱃지 텍스트 |
| Return available background | `#F1F6E8` | `귀가 가능` 뱃지 배경 |
| Return available border | `#A7BF7A` | `귀가 가능` 뱃지 테두리 |
| Return tight text | `#9B6816` | `귀가 빠듯` 뱃지 텍스트 |
| Return tight background | `#FFF4E2` | `귀가 빠듯` 뱃지 배경 |
| Return tight border | `#E1B46E` | `귀가 빠듯` 뱃지 테두리 |
| Positive | `#44B739` | 피로도 값 |
| CTA base | `#87AA37` | 코스 상세 버튼 |

- 카드에는 외곽선을 사용하지 않는다.
- CTA에는 밝은 내부 외곽선을 사용하지 않는다. 미세한 세로 그라데이션, 하단 inner shadow, 약한 외부 그림자로 깊이를 만든다.
- 순위 도장, 이미지 오버레이, 영문 대문자 장식을 추가하지 않는다.

## 4. 타이포그래피

추천 결과 화면은 Figma 기준에 따라 Pretendard Variable을 사용한다. 전역 세리프 제목 규칙보다 이 화면 규칙이 우선한다.

| 역할 | 크기 | 굵기 | 행간 / 자간 |
|---|---:|---:|---|
| 결과 제목 | 28px | 600 | normal / -2.5% |
| 순위 라벨 | 14px | 600 | normal / -2.5% |
| 카드 제목 | 20px | 600 | normal / -2.5% |
| 이미지 출처 | 12px | 400 | normal / -2.5% |
| 지표 값 | 14px | 500 | normal / -2.5% |
| 귀가 행동 안내 | 12px | 500 | 1.4 / -2.5% |
| 추천 이유 제목 | 16px | 500 | normal / -2.5% |
| 추천 기준 제목 | 14px | 600 | 44px 높이 안에서 중앙 정렬 |
| 추천 기준 본문 | 12–13px | 400–600 | 1.4–1.5 / -2.5% |
| 순위 비교 문장 | 13px | 400 | 1.5 / -2.5% |
| CTA | 14px | 600 | normal |
| 칩·지표 레이블·이유 본문 | 12px | 400 | normal / -2.5% |

- 결과 제목은 `내 조건에 맞는 코스를` / `찾았어요`의 두 줄로 고정한다.
- 제목은 단어 중간에서 자르지 않는다. 카드 제목은 최대 두 줄까지 허용한다.
- 숫자와 단위는 붙여 쓴다: `6시간`, `24분`, `1회`.
- 이 화면의 제목에 세리프를 혼용하지 않는다.

## 5. 텍스트 규칙

| 영역 | 규칙 | 예시 |
|---|---|---|
| 결과 제목 | 선택이 반영됐다는 완료형 문장 | `내 조건에 맞는 코스를 찾았어요` |
| 순위 라벨 | 점수순 순위를 짧게 표기 | `추천 1위`, `추천 2위` |
| 조건 칩 | 선택값만 짧게 표기, 조사·문장부호 없음 | `유스퀘어`, `6시간`, `자연산책` |
| 카드 제목 | 지역명 + 핵심 경험 + `코스` | `담양 느린 산책 코스` |
| 이미지 출처 | 파일명의 첫 `_` 앞 제작자명을 사용 | `사진 제공 (오경택) - 한국관광공사` |
| 지표 | `피로도`, `환승`, `총 도보`, `예상 시간` 순서 고정 | `보통`, `1회`, `24분`, `6시간` |
| 태그 | 명사형 2–5자 권장, 최대 3개 우선 노출 | `자연산책`, `감성기록`, `음식` |
| 귀가 행동 | `returnTransport.type`과 `ticketingModel`에 따라 한 문장만 표시하고 `BIS` 같은 약어는 풀어 씀 | `출발 전 버스 도착정보 확인 필요`, `왕복 교통편 사전 예약 필요` |
| 귀가 교통 기준 | 교통 유형별 회차·배차·예약 기준을 구분하고 일괄적으로 막차라 부르지 않음 | `13:30 이후 약 15분 간격` |
| 추천 이유 | 한 줄에 한 이유, 짧고 구체적인 해요체 | `자연 산책 취향과 주요 장소가 잘 맞아요` |
| 추천 기준 | 백엔드 응답의 실제 가중치를 백분율로 표시 | `취향 일치도 40%` |
| 이동 부담 세부 기준 | 도보·환승·왕복 이동 순서로 표시 | `도보 45% · 환승 30% · 왕복 이동 25%` |
| 순위 비교 | 2·3위 코스의 실제 이동 수치로 짧게 설명 | `세월옛길은 도보 28분·왕복 이동 84분으로 이동 부담이 낮아 2위가 됐어요.` |
| CTA | 행동과 목적을 함께 표기 | `코스 자세히 보기` |

- 같은 의미를 반복하지 않으며 추천 이유는 1위 3개, 2~3위 2개만 노출한다.
- 추천 이유는 가중 점수가 높은 기준부터 표시하고 `•` 불릿과 마침표를 붙인다.
- 요약은 코스명·도보시간·귀가 여유처럼 판단에 필요한 사실을 포함하고, 상세도
  같은 사실을 `~해요` 말투로 설명한다.
- `규칙을 반영했습니다`, `일정을 완성합니다`, `설계안입니다`처럼 보고서나
  생성형 문구로 느껴지는 표현은 사용하지 않는다.
- 내부 점수 설명이나 `FEASIBLE` 같은 상태 코드는 사용자에게 노출하지 않는다.
- `HIGH`, 피로도 원점수, 가중 점수와 같은 내부 코드·소수점 점수는 순위 비교에 노출하지 않는다.
- `최고`, `완벽`, `무조건` 같은 근거 없는 최상급 표현은 사용하지 않는다.

## 6. 접근성과 검수

- 뒤로가기와 CTA의 실제 터치 영역은 최소 44 × 44px을 보장한다. CTA의 시각 높이 40px은 카드 안 여백을 포함한 클릭 영역으로 보완한다.
- 이미지 대체 텍스트는 `{코스명} 대표 풍경` 형식을 사용한다.
- 제목 계층은 페이지 `h1` → 카드 `h2` → 추천 이유 `h3` 순서를 유지한다.
- 320px, 390px, 480px에서 가로 스크롤, 텍스트 겹침, 칩 잘림이 없어야 한다.
- 로딩, 빈 결과, 오류 상태와 상세 이동 기능은 스타일 변경 뒤에도 그대로 동작해야 한다.
